/** @type {import('next').NextConfig} */
const nextConfig = {
    // Configure `pageExtensions` to include only standard files
    pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
    // Optionally, add any other Next.js config below
}

export default nextConfig