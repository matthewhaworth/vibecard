import GeneratePostcard from '@/components/GeneratePostcard'

export default function GeneratePage() {
    // This is the main entry point for postcard generation
    // All generation flow should go through the GeneratePostcard component
    return <GeneratePostcard />
}